<div class="w-2/3 mx-auto block-wrapper">
  <h2> Редагувати особисту інформацію </h2>
  <%= render template: "layouts/flash", locals: { flash: flash[:error] } %>
  <%= form_with model: @user, url: personal_informations_path, method: :patch, class: "flex flex-col" do |f| %>
    <div class="input-wrapper">
      <%= f.label :email, "Електронна пошта *" %>
      <%= f.text_field :email, placeholder: "example@email.com", required: true %>
    </div>
    <div class="input-wrapper">
      <%= f.label :full_name, "Повне ім'я *" %>
      <%= f.text_field :full_name, placeholder: "Іван Шевченко", required: true %>
    </div>
    <div class="input-wrapper">
      <%= f.label :username, "Ім'я користувача *" %>
      <%= f.text_field :username, placeholder: "ivan_shevchenko", required: true %>
    </div>
    <div class="input-wrapper">
      <%= f.label :phone_number, "Телефон *" %>
      <%= f.text_field :phone_number, placeholder: "380991234141", required: true %>
    </div>
    <div class="input-wrapper">
      <%= f.label :date_of_birth, "Дата народження" %>
      <%= f.date_field :date_of_birth, as: :date %>
    </div>
    <div class="flex gap-4 mt-5 justify-end">
      <%= link_to "< Назад ", request.referrer, class: "button secondary-button" %>
      <%= f.submit "Підтвердити", class: "button primary-button" %>
    </div>
  <% end %>
</div>
